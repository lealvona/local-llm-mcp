#!/usr/bin/env bash
# The message may name no address, path, e-mail or denylisted term, may carry no attribution
# trailer, and the author/committer must be the identity configured for this repository.
set -u
here=$(cd "$(dirname "$(readlink -f "$0")")" && pwd)
exec python3 "$here/../publiccheck.py" --message "$1"
